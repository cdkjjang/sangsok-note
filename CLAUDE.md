# CLAUDE.md — 상속노트 (sangsok-note)

부모님이 남기신 것을 정리하는 데 필요한 것 — **상속세·법정상속분·기한·상속등기**를
계산기 4종과 가이드 10편으로 정리하는 애드센스 수익형 미니사이트.
생활반장 노트 시리즈의 16번째 노트.

> 경계 주의: **세금노트**는 증여세를, **부동산노트**는 매매 취득세를 다룬다.
> 이 노트는 상속 쪽만 맡는다. 세금노트의 `inheritance-tax-basics`는 개념 설명
> 가이드일 뿐 계산기가 없어 중복이 아니다. 상속 취득세는 세율표(2.8% / 무주택
> 주택 0.8%)와 과세표준(시가표준액)이 매매와 달라 별도 계산기가 맞다.
> **퇴사노트**와도 겹치지 않는다 — 유족연금은 연금노트, 사망 관련 행정 순서는
> 허브 가이드(`family-death-admin`)로 보낸다.

## 스택·명령

- Next.js 16.2.10 (App Router) + TypeScript + Tailwind CSS 4. DB·로그인·결제·API 없음, 전부 정적.
- 개발 서버: 워크스페이스 `.claude/launch.json`의 `sangsok-note-dev` (포트 4600)
- 빌드: `npm run build` / 테스트: `npm test` (vitest 70개)
- Node는 포터블: 명령 앞에 `$env:Path = "E:\클로드\tools\node;$env:Path"` 필요
- **빌드 성공 판정은 `$LASTEXITCODE -eq 0`으로 한다.** `✓ Compiled`는 타입 체크
  **전에** 출력되므로 그걸로 판정하면 타입 오류를 성공으로 오인한다.
- **PowerShell `Set-Content -Encoding utf8`은 BOM을 붙인다.** `package.json`에
  BOM이 들어가면 빌드가 통째로 깨진다. 파일을 쓸 때는
  `[System.IO.File]::WriteAllText($f, $t, (New-Object System.Text.UTF8Encoding $false))`를 쓸 것.
- 배포: `git push origin main` (Vercel 자동 배포)만 사용. 절차는 `DEPLOY.md`
- 도메인: sangsok.lifebanjang.com
- 테마: 짙은 와인(`--accent: #a03a52`) — 기존 16개 노트와 겹치지 않음

## 구조

- 계산 엔진 + 테스트 (로직 수정 시 반드시 테스트 함께 갱신):
  - `lib/inheritance-tax.ts` 상속세 — 과세가액 → 공제(일괄/배우자/금융/동거주택)
    → 과세표준 → 세율 → 신고세액공제 3%. `taxFreeCeiling()`이 "세금이 시작되는
    지점"을 역산해 홈·계산기에서 바로 보여준다.
  - `lib/legal-share.ts` 법정상속분 — 순위 판정이 먼저, 지분은 그다음.
    배제된 순위를 `excluded` 문구로 알려 주는 것이 이 계산기의 요점이다.
  - `lib/deadline.ts` 기한 D-day — 포기·한정승인 3개월(안 날 기준),
    상속세·취득세 6개월(사망월 말일 기준). 기산점이 서로 다르다.
  - `lib/registration.ts` 상속등기 비용 — 취득세 2.8% / 무주택 주택 0.8%.
- 가이드 10편: `lib/guides-1.ts`(처음 겪는 사람용 5편) · `lib/guides-2.ts`(절차·절세·분쟁 5편)
- 계산기 페이지: `app/calc/{tax,share,deadline,registration}/page.tsx`
- 허브 역링크: `components/HubGuideLink.tsx` → `lifebanjang.com/guide/family-death-admin`

## 이 노트가 반드시 설명해야 하는 것

### ① 3개월이 가장 위험한 기한이다
세금 기한은 늦으면 가산세로 끝나지만, **상속포기·한정승인 3개월은 놓치면 결과가
바뀐다.** 아무것도 하지 않으면 단순승인으로 보아 빚까지 전부 상속된다. 상속에서
유일하게 "가만히 있는 것"이 의사표시가 되는 기한이다. 이 설명을 빼지 말 것.

### ② 두 번째 상속에서 세금이 튄다
1차 상속(배우자 생존)은 일괄공제 5억 + 배우자공제 5억으로 **10억까지 세금이 없다.**
그런데 배우자가 돌아가신 뒤의 2차 상속에는 배우자공제가 없어 공제가 5억뿐이다.
같은 15억이 **8,730만원 → 2억 3,280만원**으로 뛴다. "어머니 앞으로 다 돌려놓자"가
항상 유리하지 않은 이유다.

### ③ 상속세가 0원이어도 취득세는 낸다
국세와 지방세로 창구가 다르다(홈택스 / 위택스). 이걸 몰라 가산세를 무는 경우가 많다.

## 주의사항 — 갱신 대상

> 전 노트의 갱신 항목은 워크스페이스 `E:\클로드\CLAUDE.md`의
> **"기준값 갱신 캘린더"** 참조.

- **상속세율·공제**는 세법 개정 시. 현행값은 `lib/inheritance-tax.ts` 상단에 상수로
  모아 두었고 `inheritance-tax.test.ts`에 **리터럴로 고정**해 두었다. 개정되면
  이 테스트가 먼저 깨진다.
- **상속 취득세율**은 지방세법 개정 시. `lib/registration.ts`.
- 값을 고치면 **가이드 본문(`lib/guides-*.ts`)과 계산기 해설(`app/calc/*/page.tsx`)의
  숫자**도 함께 훑을 것. `Grep`으로 옛 숫자(예: `5,000만원`, `2.8%`)를 검색한다.

## 반영하지 않은 것 (의도적)

- **유산취득세 전환** — 각자 받은 몫에 과세하는 방식으로 바꾸는 안. **2028년 시행을
  목표로 한 안이며 확정되지 않았다.** 현행 유산세 방식으로 계산한다.
- **자녀공제 5천만원 → 5억 상향** — **국회에서 부결됐다.** 현행 5천만원 유지.
- 위 둘은 워크스페이스 CLAUDE.md의 "확정되지 않은 개편안은 넣지 않는다" 원칙에
  따른 것이다. 통과되면 그때 반영할 것. 홈 FAQ와 가이드에서 "확정 전"이라고 밝힌다.
- **가업상속공제·영농상속공제, 세대생략할증, 증여세액공제, 단기재상속 세액공제** —
  요건 판정이 복잡해 계산기에 넣지 않고 가이드에서 설명만 한다.
- **기여분·특별수익·대습상속** — 법정상속분 계산기에 넣지 않는다. 입증과 재량
  판단이 필요해 숫자로 단정하면 오해를 부른다. 가이드에서 다룬다.

## 검증에 쓸 수 있는 기준값 (테스트에 들어 있음)

- 배우자 + 자녀 2, 부동산 8억 + 금융 2억 → **상속세 0원** (공제 10.4억)
- 배우자 없음, 자녀 2, 부동산 15억 → 과세표준 10억 → **2억 3,280만원**
- 같은 15억인데 배우자 생존 → **8,730만원** (1억 4,550만원 차이)
- 배우자 + 자녀 2, 재산 7억 → **3억 / 2억 / 2억** (3:2:2)
- 2026-08-10 사망 → 포기 **2026-11-10**, 세금 **2027-02-28**
- 시가표준액 5억 주택(84㎡) 유주택 → 세금 **1,480만원** / 무주택 → **480만원**

## 그 밖에

- 브라우저 스크린샷은 이 환경에서 타임아웃 가능 — get_page_text/read_page/javascript_tool로 검증.
- PowerShell에서 `app/guide/[slug]` 경로는 `-LiteralPath` 사용. `&&` 불가.
- `components/FamilyLinks.tsx`의 SITES 배열은 전 사이트 공통 정본.
- **가이드를 추가·수정하면** 허브에서 `npm run gen:note-guides`를 돌리고 허브도 함께
  배포해야 `lifebanjang.com/articles/sangsok` 목록에 반영된다.
- 입력한 재산·날짜는 브라우저에서만 처리하고 서버로 보내지 않는다.
- **세무·법률 자문이 아니라는 면책**과 "확정은 국세청·전문가 확인"이라는 고지를 유지할 것.
  상속은 분쟁이 잦은 영역이라 단정적인 표현을 피한다.
